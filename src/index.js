import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // Impor komponen App dari file App.js

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
